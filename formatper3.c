#include <stdio.h> //library file of code we can use
#include <cs50.h>

int main(void){

    int age = 17; //declaring and initializing our variables
    float gpa = 4.5;
    string name = "Nell";

    printf("Her name is %s\n", name);
    printf("Her gpa is %.2f\n", gpa);
    printf("Her age is %i\n", age);
}